/*1.-Create a C++ function using the stack STL to evaluate a postfix expression. Assume
the postfix expression is given to your program in a string vector and assume you can use
the atoi() function to turn an ASCII C style string into an integer. The function must
return error codes on failures or the integer result on success. Assume the only valid
operations are: +, - and *. Assume you have the functions isInteger(string) and
isOperator(string) to identify integers and operators. (10 points)*/


#include <iostream>
#include<stack>
#include <stdlib.h>

using namespace std;

int evaluate(const vector<string> &S){
	stack<int> S;
	for(unsigned int i = 0; i < S.size(); i++){
	if(S[i])

	}
}

int main(){

	return 0;
}
